// Civic Voice - deployment configuration
// Set the live backend API base URL here for cloud deployments.
// Leave as-is for local development (api.js falls back to http://localhost:8080/api).
window.__API_BASE__ = window.__API_BASE__ || null;
